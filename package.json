{
  "name": "grab-rescue",
  "version": "1.0.0",
  "description": "This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.",
  "main": "index.js",
  "scripts": {
    "dev": "npm install && concurrently \"cd backend && npm install && npm run dev\" \"cd frontend && npm install && npm run dev\" ",
    "host": "npm install && concurrently \"cd backend && npm install && npm run dev\" \"cd frontend && npm install && npm run host\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@headlessui/react": "^2.1.10",
    "concurrently": "^8.2.2",
    "jspdf": "^2.5.2",
    "jspdf-autotable": "^3.8.4",
    "multer": "^1.4.5-lts.1"
  }
}
